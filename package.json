// package.json - Abhängigkeiten (optional)
{
  "name": "magnet-shortener",
  "version": "1.0.0",
  "description": "Magnet Link Shortener with Vercel",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}